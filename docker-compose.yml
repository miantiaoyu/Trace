services:
  trace:
    build:
      context: .
    image: trace-api-probe:local
    user: "10001:10001"
    init: true
    environment:
      TRACE_ENV: ${TRACE_ENV:-test}
    restart: "no"
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=256m
      - /home/trace/.cache:rw,noexec,nosuid,size=128m,mode=700
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    mem_limit: 1g
    cpus: 2.0
    secrets:
      - source: trace_db
        target: trace-db.yml
    volumes:
      - trace_state:/var/lib/trace
    command:
      - --db-config
      - /run/secrets/trace-db.yml
      - --days
      - "7"
      - --limit
      - "20"
      - --lock-file
      - /var/lib/trace/.trace-api-probe.lock
      - --run-log
      - /var/lib/trace/trace-runs.jsonl
      - --health-state
      - /var/lib/trace/trace-health.json
      - --alert-threshold
      - "3"

secrets:
  trace_db:
    file: ${TRACE_DB_CONFIG:-./test-db.yml}

volumes:
  trace_state:
